version: 2.1

orbs:
  aws-cli: circleci/aws-cli@5

parameters:
  run-component-tests:
    description: If true, runs backend component tests. Always runs on main branch.
    type: boolean
    default: false
  delete-services:
    description: If true, deletes the Terraform resources in AWS.
    type: boolean
    default: false

terraform_version: &terraform_version "1.14.5"
terraform_docker_image: &terraform_docker_image "hashicorp/terraform:1.14"

executors:
  node-medium-executor:
    docker:
      - image: cimg/node:24.14
    resource_class: medium
  node-small-executor:
    docker:
      - image: cimg/node:24.14
    resource_class: small

workflows:
  build-deploy:
    when:
      and:
        - not: << pipeline.parameters.delete-services >>
        - not: << pipeline.parameters.run-component-tests >>
    jobs:
      - check-format-files
      - lint-typescript-code
      - build-backend
      - run-backend-unit-tests
      - run-frontend-tests
      - lint-terraform-code
      - deploy-backend-and-infrastructure:
          requires:
            - check-format-files
            - lint-typescript-code
            - build-backend
            - run-backend-unit-tests
            - lint-terraform-code
      # Run component tests only on main branch
      - run-backend-component-tests:
          requires:
            - deploy-backend-and-infrastructure
          filters:
            branches:
              only:
                - main
      - build-frontend:
          requires:
            - deploy-backend-and-infrastructure
      - deploy-frontend:
          requires:
            - check-format-files
            - lint-typescript-code
            - build-frontend
            - run-frontend-tests
            - deploy-backend-and-infrastructure

  component-tests:
    when:
      and:
        - not: << pipeline.parameters.delete-services >>
        - << pipeline.parameters.run-component-tests >>
    jobs:
      - run-backend-component-tests

  delete-services:
    when: << pipeline.parameters.delete-services >>
    jobs:
      - terraform-destroy

jobs:
  check-format-files:
    executor: node-small-executor
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm ci
      - install-terraform
      - run:
          name: Check file formatting
          command: npm run check-format

  lint-typescript-code:
    executor: node-small-executor
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm ci
      - run:
          name: Lint TypeScript code
          command: npm run lint

  build-backend:
    executor: node-medium-executor
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm ci
      - run:
          name: Build api-spec package
          command: |
            cd api-spec
            npm run build
      - run:
          name: Build backend
          command: |
            cd backend
            npm run build
      - persist_to_workspace:
          root: ~/project
          paths:
            - backend/build

  run-backend-unit-tests:
    executor: node-medium-executor
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm ci
      - run:
          name: Build api-spec package
          command: |
            cd api-spec
            npm run build
      - run:
          name: Run unit tests
          command: |
            cd backend
            npm run test:unit

  run-frontend-tests:
    executor: node-medium-executor
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm ci
      - run:
          name: Build api-spec package
          command: |
            cd api-spec
            npm run build
      - run:
          name: Run frontend tests
          command: |
            cd frontend
            npm run test

  lint-terraform-code:
    executor: node-small-executor
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm run install-lint-terraform
      - run:
          name: Lint Terraform code
          command: npm run lint:terraform

  run-backend-component-tests:
    executor: node-medium-executor
    steps:
      - checkout
      - aws-cli/setup:
          role_arn: "$AWS_ROLE_ARN"
      - run:
          name: Install dependencies
          command: npm ci
      - install-terraform
      - run:
          name: Build api-spec package
          command: |
            cd api-spec
            npm run build
      - run:
          name: Set environment variables for component tests
          command: |
            cd terraform
            terraform init -input=false

            # Create environment file for component tests
            echo "export COMPONENT_TESTS_COGNITO_REGION=$(terraform output aws_region)" >> terraform_output
            echo "export COMPONENT_TESTS_COGNITO_APP_CLIENT_ID=$(terraform output cognito_app_client_id)" >> terraform_output
            echo "export COMPONENT_TESTS_API_BASE_URL=$(terraform output api_versioned_url)" >> terraform_output
      - run:
          name: Run component tests
          command: |
            # Set environment variables for tests
            source ~/project/terraform/terraform_output

            cd backend
            npm run test:component

  # https://developer.hashicorp.com/terraform/tutorials/automation/circle-ci
  deploy-backend-and-infrastructure:
    docker:
      - image: *terraform_docker_image
    resource_class: medium
    steps:
      - checkout
      - attach_workspace:
          at: ~/project
      - aws-cli/setup:
          role_arn: "$AWS_ROLE_ARN"
      - run:
          name: Terraform init
          command: |
            cd terraform
            terraform init -input=false
      - run:
          name: Terraform plan
          command: |
            cd terraform
            terraform plan -out tfapply
      - run:
          name: Terraform apply
          command: |
            cd terraform
            terraform apply -auto-approve tfapply
      - run:
          name: Persist Terraform output for frontend
          command: |
            cd terraform

            echo "export VITE_COGNITO_REGION=$(terraform output aws_region)" >> terraform_output
            echo "export VITE_COGNITO_APP_CLIENT_ID=$(terraform output cognito_app_client_id)" >> terraform_output
            echo "export VITE_API_BASE_URL=$(terraform output api_versioned_url)" >> terraform_output
            echo "export FRONTEND_BUCKET_NAME=$(terraform output frontend_bucket_name)" >> terraform_output
            echo "export FRONTEND_BUCKET_REGION=$(terraform output aws_region)" >> terraform_output
      - persist_to_workspace:
          root: ~/project
          paths:
            - terraform/terraform_output

  build-frontend:
    executor: node-medium-executor
    steps:
      - checkout
      - attach_workspace:
          at: ~/project
      - run:
          name: Install dependencies
          command: npm ci
      - run:
          name: Build api-spec package
          command: |
            cd api-spec
            npm run build
      - run:
          name: Build frontend
          command: |
            source ~/project/terraform/terraform_output
            cd frontend
            npm run build
      - persist_to_workspace:
          root: ~/project
          paths:
            - frontend/dist

  deploy-frontend:
    executor: node-small-executor
    steps:
      - attach_workspace:
          at: ~/project
      - aws-cli/setup:
          role_arn: "$AWS_ROLE_ARN"
      - run:
          name: Deploy frontend to S3
          command: |
            source ~/project/terraform/terraform_output
            aws s3 sync ~/project/frontend/dist s3://$FRONTEND_BUCKET_NAME --delete --region $FRONTEND_BUCKET_REGION

  terraform-destroy:
    docker:
      - image: *terraform_docker_image
    resource_class: small
    steps:
      - checkout
      - aws-cli/setup:
          role_arn: "$AWS_ROLE_ARN"
      - run:
          name: Terraform init
          command: |
            cd terraform
            terraform init -input=false
      - run:
          name: Terraform plan destroy
          command: |
            cd terraform
            terraform plan -destroy -out tfdestroy
      - run:
          name: Terraform destroy
          command: |
            cd terraform
            terraform apply -auto-approve tfdestroy

commands:
  install-terraform:
    description: Install Terraform using the version specified in terraform_version anchor
    parameters:
      version:
        type: string
        default: *terraform_version
    steps:
      - run:
          name: Install Terraform
          command: |
            TF_VER=<< parameters.version >>
            TEMP_DIR=$(mktemp -d)
            curl --fail --location --output ${TEMP_DIR}/terraform.zip https://releases.hashicorp.com/terraform/${TF_VER}/terraform_${TF_VER}_linux_amd64.zip
            unzip ${TEMP_DIR}/terraform.zip -d ${TEMP_DIR}
            sudo mv ${TEMP_DIR}/terraform /usr/local/bin/
            terraform -version
            rm -rf ${TEMP_DIR}

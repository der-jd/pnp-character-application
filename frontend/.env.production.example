# Production Environment Configuration for API Schema Testing
# Copy this file and update with your actual production values
# ⚠️  USE WITH EXTREME CAUTION - THIS TESTS AGAINST PRODUCTION DATA

# Production API Gateway URL (replace with your actual production API Gateway)
PROD_API_BASE_URL=https://your-prod-api-id.execute-api.eu-central-1.amazonaws.com/prod

# Production API Key (if using API Gateway API Keys)
PROD_API_KEY=your-production-api-key-here

# Production Authentication Token (JWT token for testing)
# This should be a READ-ONLY test user token in production
PROD_AUTH_TOKEN=your-production-jwt-token-here

# Production specific configuration
PROD_REGION=eu-central-1
PROD_TIMEOUT=30000

# Test data configuration for production
# ⚠️  Use dedicated test data that won't affect real users
PROD_TEST_CHARACTER_ID=your-prod-test-character-id
PROD_TEST_USER_ID=your-prod-test-user-id

# Safety configuration
# Set to "true" to enable production testing (requires manual confirmation)
PROD_TESTING_ENABLED=false